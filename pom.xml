<?xml version="1.0" encoding="UTF-8"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"
         xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>fr.openent</groupId>
        <artifactId>app-parent</artifactId>
        <version>1.2.0</version>
    </parent>
    <groupId>fr.openent</groupId>
    <artifactId>viescolaire</artifactId>
    <version>2.1.5-patched</version>
    <scm>
        <connection>scm:git:https://github.com/OPEN-ENT-NG/vie-scolaire.git</connection>
        <developerConnection>scm:git:https://github.com/OPEN-ENT-NG/vie-scolaire.git</developerConnection>
        <url>https://github.com/OPEN-ENT-NG/vie-scolaire</url>
    </scm>
    <repositories>
        <repository>
            <id>ode</id>
            <name>ODE Repository</name>
            <url>https://maven.opendigitaleducation.com/nexus/content/groups/public</url>
        </repository>
    </repositories>
    <properties>
        <toolsVersion>2.0.0-final</toolsVersion>
        <junitVersion>5.1.0</junitVersion>
        <powerMockVersion>2.0.2</powerMockVersion>
        <mockitoVersion>2.28.2</mockitoVersion>
        <entCoreVersion>6.14.9-patched</entCoreVersion>
        <reflectionsVersion>0.10.2</reflectionsVersion>
        <opencsvVersion>4.0</opencsvVersion>
    </properties>
    <dependencies>
        <dependency>
            <groupId>io.vertx</groupId>
            <artifactId>vertx-core</artifactId>
            <version>${vertxVersion}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>io.vertx</groupId>
            <artifactId>vertx-web-client</artifactId>
            <version>${vertxVersion}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.entcore</groupId>
            <artifactId>common</artifactId>
            <version>${entCoreVersion}</version>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>com.opencsv</groupId>
            <artifactId>opencsv</artifactId>
            <version>${opencsvVersion}</version>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>io.vertx</groupId>
            <artifactId>vertx-codegen</artifactId>
            <version>${vertxVersion}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.vertx</groupId>
            <artifactId>vertx-unit</artifactId>
            <version>${vertxVersion}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <version>${junitVersion}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <version>${mockitoVersion}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.powermock</groupId>
            <artifactId>powermock-core</artifactId>
            <version>${powerMockVersion}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.powermock</groupId>
            <artifactId>powermock-api-mockito2</artifactId>
            <version>${powerMockVersion}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.powermock</groupId>
            <artifactId>powermock-module-junit4</artifactId>
            <version>${powerMockVersion}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.powermock</groupId>
            <artifactId>powermock-module-testng</artifactId>
            <version>${powerMockVersion}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.reflections</groupId>
            <artifactId>reflections</artifactId>
            <version>${reflectionsVersion}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <profiles>
        <!--
            org.entcore:tests ne fournit QUE les scénarios Gatling/Scala (src/test/scala/**),
            jamais joués en CI. Depuis le 08/09/2026, le module `tests` d'entcore est exclu de la
            publication du fork (open-ent/entcore-v2, commit 275a060 : maven-scala-plugin 2.15.2 ne
            compile plus Scala 2.12 sous JDK 21), donc org.entcore:tests:*-patched n'existe plus.
            Maven résout les dépendances de portée `test` même avec -Dmaven.test.skip=true : laissée
            dans <dependencies>, elle faisait échouer `mvn package` (donc toute la CI).
            `mvn -Pgatling-it ...` pour la réactiver le jour où l'artefact sera republié.
        -->
        <profile>
            <id>gatling-it</id>
            <dependencies>
                <dependency>
                    <groupId>org.entcore</groupId>
                    <artifactId>tests</artifactId>
                    <version>${entCoreVersion}</version>
                    <scope>test</scope>
                </dependency>
            </dependencies>
        </profile>
    </profiles>
</project>
